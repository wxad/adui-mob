<view class="{{prefix}}" style="min-height: {{minHeight}}; {{customStyle}}">
	<view class="{{prefix}}_body">
		<slot name="body" />
	</view>
	<view class="{{prefix}}_footer">
		<slot name="footer" />
	</view>
</view>

