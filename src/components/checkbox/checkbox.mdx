---
name: Checkbox
menu: 基础类
index: 6
---

import { Alert, Table } from 'adui'

# Checkbox

支持同时选中多项的基础控件。

<Alert
  title="组件间关系"
  text="checkboxGroup 组件的子孙组件，可搭配 checkboxGroup 使用，搭配 checkboxGroup 使用时，每个 checkbox 需要提供唯一的标识 value，也可单独使用"
/>

## Slot

export const slots = [
  {
    name: '',
    desc: '默认 slot, 可用于给 checkbox 增加文案',
  },
]

<Table
  columnsResizable
  dataSource={slots}
  columns={[
    {
      dataIndex: 'name',
      title: 'name',
    },
    {
      dataIndex: 'desc',
      title: '描述',
    },
  ]}
/>

## Properties
